{
  "name": "slack-archiver",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight Slack message archiver and search tool for small workspaces.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.1075.0",
    "@aws-sdk/client-ssm": "^3.1075.0",
    "@aws-sdk/lib-dynamodb": "^3.1075.0",
    "jose": "^6.2.3"
  }
}
